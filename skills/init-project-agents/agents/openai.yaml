interface:
  display_name: "Init Project Agents"
  short_description: "Initialize Git, ignore rules, agents folders, and guide files"
  default_prompt: "Use $init-project-agents to prepare this project directory for agent-local skills."
